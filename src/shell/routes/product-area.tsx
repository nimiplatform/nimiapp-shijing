/**
 * The production entrypoint does not import this dormant product route.
 * ShiJing product operations remain unavailable until their protected
 * installed-session operation set is independently admitted.
 */
export function ProductArea(): null {
  return null;
}
