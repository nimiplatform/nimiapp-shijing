# 时镜 ShiJing

结合四柱与节气等确定性排盘，提供个人节律观察与解读。

## 使用方式

1. 建立自己的资料，确认排盘所需的出生信息。
2. 通过日镜、月镜、年镜观察不同时间跨度的节律。
3. 在命镜查看个人结构，在合镜围绕自己与一位对象进行观察。
4. 在问镜中结合已有解读继续提问，并保留有用的观察与记录。

排盘由确定性计算完成，AI 用于表达与解释。生成失败时会显示实际状态，不会伪造解读。

## 运行条件

在 Nimi 的应用列表中安装或导入此应用，再点击启动。应用由 Nimi 启动和管理；运行状态、App Access 和所需能力以 Nimi 的实际状态为准。

使用 AI 功能前，在时镜的“设置 → 时镜 AI 配置”中点击“配置 AI 模型”，为文本生成选择可用模型并保存。若没有可用模型，先在 Nimi 中准备本机模型或配置云端连接器，再返回时镜完成选择。缺少所需模型或能力时，相关功能会报告不可用，不会生成替代结果。

## 数据与支持

应用数据通过 Nimi 的应用私有存储保存。

[使用与开发说明](https://github.com/nimiplatform/nimiapp-shijing) · [反馈问题](https://github.com/nimiplatform/nimiapp-shijing/issues)
